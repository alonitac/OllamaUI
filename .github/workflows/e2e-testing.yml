
#e2e-testing.yml




name: Simple Deploy to EC2

on:
  pull_request:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.build_push.outputs.image_tag }}  # <-- נעשה expose של ה-TAG ל-jobs אחרים
    steps:
      - name: Get code
        uses: actions/checkout@v4

      - name: Build and push Docker image
        id: build_push
        run: |
          echo ${{ secrets.DOCKERHUB_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin
          TAG="${{ github.sha }}-$(date +%Y%m%d-%H%M%S)"
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/ollama-ui:$TAG .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/ollama-ui:$TAG
          echo "IMAGE_TAG=$TAG" >> $GITHUB_ENV
          echo "image_tag=$TAG" >> $GITHUB_OUTPUT   # <-- זה מה שיעבור ל-job הבא

      - name: Copy docker-compose to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "docker-compose.yml"
          target: "/home/ubuntu/"

      - name: Deploy on EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -euo pipefail
            cd /home/ubuntu
            sudo apt-get remove -y podman-docker podman || true
            if ! command -v docker >/dev/null 2>&1; then
              sudo apt-get update -y
              sudo apt-get install -y ca-certificates curl gnupg
              sudo install -m 0755 -d /etc/apt/keyrings
              curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
              echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release; echo $VERSION_CODENAME) stable" \
                | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
              sudo apt-get update -y
              sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
              sudo systemctl enable --now docker
            fi

            echo "${{ secrets.DOCKERHUB_PASSWORD }}" | sudo docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
            sudo mkdir -p uploads

            cat > .env <<EOF
            DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
            OLLAMA_UI_IMG_TAG=${{ env.IMAGE_TAG }}
            EOF

            sudo docker compose --env-file .env pull
            sudo docker compose --env-file .env down || true
            sudo docker compose --env-file .env up -d
            sudo docker compose --env-file .env ps

  selenium-matrix-tests:
    runs-on: ubuntu-latest
    needs: deploy
    strategy:
      matrix:
        browser: [chrome, firefox]
        resolution:
          - { name: "desktop-full", width: 1920, height: 1080 }
          - { name: "laptop",       width: 1366, height: 768 }
          - { name: "tablet",       width: 1024, height: 768 }
          - { name: "mobile",       width: 375,  height: 667 }
      fail-fast: false
    name: Test ${{ matrix.browser }} - ${{ matrix.resolution.name }}

    steps:
      - name: Checkout OllamaUITesting
        uses: actions/checkout@v4
        with:
          repository: tmeraslan/OllamaUITesting
          # אם הפרויקט פרטי – השתמש ב-PAT בתור secret למשל GH_PAT:
          # token: ${{ secrets.GH_PAT }}
          path: .
          clean: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install allure-pytest

      # מתקינים דפדפן בצורה מהירה ויציבה:
      - name: Setup Chrome
        if: matrix.browser == 'chrome'
        uses: browser-actions/setup-chrome@v1

      - name: Setup Firefox
        if: matrix.browser == 'firefox'
        uses: browser-actions/setup-firefox@v1

      # מחכים שהאפליקציה ב-EC2 תהיה זמינה
      - name: Wait for app to be ready
        run: |
          URL="http://${{ secrets.EC2_HOST }}:3000/"
          for i in {1..60}; do
            if curl -fsS "$URL" >/dev/null; then
              echo "App is up: $URL"
              exit 0
            fi
            echo "Waiting for app... ($i)"
            sleep 5
          done
          echo "App not ready at $URL" >&2
          exit 1

      - name: Create Allure environment properties
        run: |
          mkdir -p allure-results
          cat > allure-results/environment.properties << EOF
          Browser=${{ matrix.browser }}
          Resolution=${{ matrix.resolution.name }}
          Screen.Width=${{ matrix.resolution.width }}
          Screen.Height=${{ matrix.resolution.height }}
          Test.Environment=${{ matrix.browser }}-${{ matrix.resolution.name }}
          Ollama.URL=http://${{ secrets.EC2_HOST }}:3000/
          Headless.Mode=true
          Ollama.Image=${{ secrets.DOCKERHUB_USERNAME }}/ollama-ui:${{ needs.deploy.outputs.image_tag }}
          YOLO.Image=yosefshawah/yolo:0.0.3
          EOF

      # מוסיפים conftest.py עם פרמטרים ל-Allure (פשוט תמיד כותבים קובץ)
      - name: Inject Allure matrix params
        run: |
          cat > conftest.py << 'PY'
          import os, pytest
          try:
              import allure
          except Exception:
              allure = None

          @pytest.fixture(autouse=True)
          def _allure_add_matrix_params():
              if not allure:
                  return
              browser = os.getenv('BROWSER', 'unknown')
              res_name = os.getenv('TEST_NAME', 'unknown')
              width = os.getenv('SCREEN_WIDTH', '')
              height = os.getenv('SCREEN_HEIGHT', '')
              try:
                  allure.dynamic.parameter('browser', browser)
                  allure.dynamic.parameter('resolution', res_name)
                  if width and height:
                      allure.dynamic.parameter('screen', f"{width}x{height}")
              except Exception:
                  pass
          PY

      - name: Run Selenium tests
        env:
          HEADLESS: true
          BROWSER: ${{ matrix.browser }}
          SCREEN_WIDTH: ${{ matrix.resolution.width }}
          SCREEN_HEIGHT: ${{ matrix.resolution.height }}
          OLLAMA_URL: http://${{ secrets.EC2_HOST }}:3000/
          TEST_NAME: ${{ matrix.browser }}-${{ matrix.resolution.name }}
          ALLURE_RESULTS_DIR: allure-results
        run: |
          pytest tests/ -v --tb=short --alluredir=allure-results

      - name: Upload Allure Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-${{ matrix.browser }}-${{ matrix.resolution.name }}
          path: allure-results/
          retention-days: 30

















# name: Simple Deploy to EC2

# on:
#   pull_request:
#     branches: [ master ]   # ודא שזה באמת branch ברירת המחדל שלך

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Get code
#         uses: actions/checkout@v4

#       - name: Build and push Docker image
#         run: |
#           # Login to DockerHub
#           echo ${{ secrets.DOCKERHUB_PASSWORD }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

#           TAG="${{ github.sha }}-$(date +%Y%m%d-%H%M%S)"

#           # בונים ודוחפים ל-<user>/ollamaui (לא ollama)
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/ollama-ui:$TAG .
#           docker push ${{ secrets.DOCKERHUB_USERNAME }}/ollama-ui:$TAG

#           # שומרים את ה-TAG לשימוש בשלבים הבאים
#           echo "IMAGE_TAG=$TAG" >> $GITHUB_ENV

#       - name: Copy docker-compose to EC2
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "docker-compose.yml"     # ודא שהשם בקוד הוא .yml
#           target: "/home/ubuntu/"

#       - name: Deploy on EC2 via SSH
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             set -euo pipefail
#             cd /home/ubuntu

#             # מסירים podman-docker במקרה והוא מותקן
#             sudo apt-get remove -y podman-docker podman || true

#             # מתקינים Docker Engine + Compose v2 אם צריך
#             if ! command -v docker >/dev/null 2>&1; then
#               sudo apt-get update -y
#               sudo apt-get install -y ca-certificates curl gnupg
#               sudo install -m 0755 -d /etc/apt/keyrings
#               curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
#               echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
#               https://download.docker.com/linux/ubuntu $(. /etc/os-release; echo $VERSION_CODENAME) stable" | \
#                 sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
#               sudo apt-get update -y
#               sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
#               sudo systemctl enable --now docker
#             fi

#             # Login ל-Docker Hub על השרת (גם לפאבליק זה מזרז/מונע rate-limit)
#             echo "${{ secrets.DOCKERHUB_PASSWORD }}" | sudo docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

#             # תקיית mount ל-uploads אם נדרש ע"י compose
#             sudo mkdir -p uploads

#             # קובץ .env לאינטרפולציה ב-compose
#             cat > .env <<EOF
#             DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
#             OLLAMA_UI_IMG_TAG=${{ env.IMAGE_TAG }}
#             EOF

#             # דיפלוי
#             sudo docker compose --env-file .env pull
#             sudo docker compose --env-file .env down || true
#             sudo docker compose --env-file .env up -d
#             sudo docker compose --env-file .env ps


 


























    # - name: Copy docker-compose file to EC2
    #   uses: appleboy/scp-action@v0.1.7
    #   with:
    #         host: ${{ secrets.EC2_HOST }}
    #         username: ubuntu
    #         key: ${{ secrets.EC2_SSH_KEY }}
    #         source: "docker-compose.yml"     # make sure the file name matches your repo
    #         target: "/home/ubuntu/"

    # - name: Deploy to EC2
    #   uses: appleboy/ssh-action@v1.0.3
    #   with:
    #         host: ${{ secrets.EC2_HOST }}
    #         username: ubuntu
    #         key: ${{ secrets.EC2_SSH_KEY }}
    #         # optionally pass envs: IMAGE_TAG  (לא חובה אם משתמשים בהרחבה למטה)
    #         script: |
    #             set -euo pipefail
    #             cd /home/ubuntu

    #             # Write .env for compose interpolation (values rendered by GitHub on the runner)
    #             cat > .env <<EOF
    #             DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
    #             OLLAMA_UI_IMG_TAG=${{ env.IMAGE_TAG }}
    #             EOF

    #             # Bring the stack up (Compose v2)
    #             sudo docker compose --env-file .env pull || true
    #             sudo docker compose --env-file .env down || true
    #             sudo docker compose --env-file .env up -d

    #             sudo docker compose --env-file .env ps
















# name: Deploy to EC2

# on:
#   pull_request:
#     branches: [master]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Build and push Docker image
#         run: |
#           # Login to DockerHub
#           echo ${{ secrets.DOCKERHUB_TOKEN }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

#           TAG="${{ github.sha }}-$(date +%Y%m%d-%H%M%S)"

#           # Build the image with the tag
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/ollama-ui:$TAG .

#           # Push the image to DockerHub
#           docker push ${{ secrets.DOCKERHUB_USERNAME }}/ollama-ui:$TAG

#           # Save the tag for later steps
#           echo "IMAGE_TAG=$TAG" >> $GITHUB_ENV

#       - name: Copy docker-compose file to EC2
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "docker-compose.yml"
#           target: "/home/ubuntu/"

#       - name: Deploy to EC2
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             # Stop and remove all containers (force cleanup)
#             docker-compose down || true
#             docker stop $(docker ps -aq) 2>/dev/null || true
#             docker rm $(docker ps -aq) 2>/dev/null || true

#             # Remove all images to free space
#             docker rmi $(docker images -q) 2>/dev/null || true

#             # Clean up dangling volumes and networks
#             docker volume prune -f || true
#             docker network prune -f || true
#             docker system prune -f || true

#             # Create .env file with the new image tag
#             cat > .env << EOF
#             OLLAMA_UI_IMG_TAG=${{ env.OLLAMA_UI_IMG }}
#             DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
#             EOF

#             # Pull the new image and start services
#             docker-compose pull
#             docker-compose up -d












# name: Simple Deploy to EC2

# on:
#   pull_request:
#     branches: [ master ]



# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Get code
#       uses: actions/checkout@v4

#     - name: Login to Docker Hub
#       run: |
#         echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

#     - name: Build & push OllamaUI image
#       id: build
#       shell: bash
#       run: |
#         TAG="${GITHUB_SHA}-$(date +%Y%m%d-%H%M%S)"
#         IMAGE="${{ secrets.DOCKERHUB_USERNAME }}/ollama:${TAG}"

#         echo "Building ${IMAGE} ..."
#         docker build -t "${IMAGE}" .

#         echo "Pushing ${IMAGE} ..."
#         docker push "${IMAGE}"

#         # expose TAG and full image ref to later steps in THIS job
#         echo "TAG=${TAG}"            >> "$GITHUB_OUTPUT"
#         echo "IMAGE_FULL=${IMAGE}"   >> "$GITHUB_OUTPUT"

#     - name: Copy docker-compose.yml to EC2
#       uses: appleboy/scp-action@v0.1.7
#       with:
#         host: ${{ secrets.EC2_HOST }}
#         username: ubuntu                  # <-- fixed (was $ubuntu)
#         key: ${{ secrets.EC2_SSH_KEY }}
#         source: "docker-compose.yml"
#         target: "/home/ubuntu/"

#     - name: Deploy on EC2
#       uses: appleboy/ssh-action@v1.0.3
#       with:
#         host: ${{ secrets.EC2_HOST }}
#         username: ubuntu
#         key: ${{ secrets.EC2_SSH_KEY }}
#         script: |
#           set -euo pipefail
#           cd /home/ubuntu

#           # Write/override .env for Compose (Compose auto-loads .env in this dir)
#           cat > .env <<EOF
#           # Full image reference for the UI (repo:tag)
#           OLLAMA_UI_IMG=${{ steps.build.outputs.IMAGE_FULL }}
#           EOF

#           echo "Using OLLAMA_UI_IMG=$(grep '^OLLAMA_UI_IMG' .env | cut -d= -f2-)"

#           # Bring down old stack (ignore if not running), pull, then start
#           docker compose down || true
#           docker compose pull || true
#           docker compose up -d

#           # Optional: show status
#           docker compose ps

