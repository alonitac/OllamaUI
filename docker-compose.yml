services:
  yolo:
    image: ${YOLO_IMAGE}
    ports:
      - "${YOLO_PORT}:${YOLO_PORT}"
    # volumes:
    #   - ./uploads:/app/uploads

  ollama-ui:
    image: ${OLLAMA_UI_IMAGE}
    ports:
      - "${OLLAMA_UI_PORT}:${OLLAMA_UI_PORT}"
